var JSON;

/**
 * @param {*} obj
 * @param {function(string, *)=} replacer
 * @param {*=} pretty
 */
JSON.stringify = function(obj, replacer, pretty){};

/**
 * @param {string} json
 */
JSON.parse = function(json){};
